[package]
name = "oauth2-server"
version = "0.1.0"
authors = ["Andreas Molzer <andreas.molzer@gmx.de>"]

[dependencies]
chrono = "0.4"
url = "*"
iron = { version = "*", optional = true }

[features]
default = ["iron-backend"]
iron-backend = ["iron"]

[dev-dependencies]
router = "*"
reqwest = "*"
